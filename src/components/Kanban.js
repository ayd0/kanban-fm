export default function Kanban() {
    return (
        <div id="kanban-container">
            <h2>This board is empty. Create a new column to get started.</h2>
            <button><img src="./assets/icons/icon-add-task-mobile.svg" />Add New Column</button>
        </div>
    );
}
